from .Attribute import Attribute

class UType(Attribute):
    """
    UType class is a subclass of Attribute that represents a user-defined type (UDT) in the database.
    It is used to define custom data types that can be used in the database schema.
    """
    